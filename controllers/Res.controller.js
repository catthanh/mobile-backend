module.exports = {
    dosomething: async (req, res, next) => {
    //     try {
    //         const result = await resSchema.validateAsync(req.body);
    //         console.log(result);
    //         res.send({ result });
    //     } catch (error) {
    //         if (error.isJoi === true) error.status = 422;
    //         next(error);
    //     }
     },
};